# monkey

This file is a router. It holds no reference content.

**Read `docs/agents.md` first, before any code work in this repo.** It carries the
architecture, the JNI contract, the manifest invariants that fail silently, and the
performance budget.

| Doc | Read it when |
| --- | --- |
| `docs/agents.md` | Any code change. Mandatory first read. |
| `README.md` | Human-facing overview, install steps, hardware caveats. |
| `app/src/main/cpp/grid.h` | Changing layout, scroll feel, or hit-testing. |
