<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme, not AppCompat: keeps the APK free of AndroidX entirely. -->
    <style name="Theme.MonkeyShell" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:windowDisablePreview">false</item>
    </style>
</resources>
